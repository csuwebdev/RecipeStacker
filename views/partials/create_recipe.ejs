  <div class="create-form">
    Recipe Name:
    <input ng-model="recipeName" placeholder="recipe name" autofocus>
    UserName:
    <input ng-model="userName" placeholder="username"><br />
    Ingredients: {{ingredients.length}}
    <button ng-show="ingredients.length<maxIngredients" ng-click="ingredients[ingredients.length] = currentIngredient">+</button>
    <button ng-show="ingredients.length>0" ng-click="ingredients.splice(ingredients.length,ingredients.length)">-</button><br />
    <input ng-model="currentIngredient.name" placeholder="ingredient name" required> 
    <input ng-model="currentIngredient.quantity" placeholder="quantity" required>
    <input ng-model="currentIngredient.unit" placeholder="unit"> <br />
    <span class="ingredientTableHeader">Ingredients:</span>
    <table>
    <tr ng-repeat="ingredient in ingredients">
        <td class="ingredientTableData">{{ingredient.name}}</td>
        <td class="ingredientTableData">{{ingredient.quantity}}</td>
        <td class="ingredientTableData">{{ingredient.unit}}</td>
    </tr>
    </table>
    <form novalidate class="simple-form" action="#/search" method="get">
    <button ng-click="inputRecipe(recipeName)" type=submit>Create</button>(This currently re-directs to "index/search" for testing purposes after the object is posted to the db")
    </form>
  </div>


